[tool:pytest]
minversion = 6.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --disable-warnings
    -v
    --tb=short
markers =
    slow: marks tests as slow
    gpu: marks tests as requiring GPU
